# `SliderSingle` component
The SliderSingle component allows the user to select a single value within a given range by moving a slider along a track.

### Storybook
You can view the `SliderSingle` component in the Storybook.